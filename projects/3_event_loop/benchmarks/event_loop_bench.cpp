#include <benchmark/benchmark.h>

#include "event_loop.hpp"
//
// static void BM_Answer(benchmark::State& state) {
//   for (auto _ : state) {
//     auto v = event_loop::answer();
//     benchmark::DoNotOptimize(v);
//   }
// }
// BENCHMARK(BM_Answer);
